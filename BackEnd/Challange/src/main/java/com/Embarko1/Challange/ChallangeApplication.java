package com.Embarko1.Challange;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ChallangeApplication {

	public static void main(String[] args) {
		SpringApplication.run(ChallangeApplication.class, args);
	}
}

//JPA provides functionality, so we can convert data into columns and class into tables and store and save data
//jpa allows us to convert entities into tables && H2 is database